#Fri May 24 09:40:47 CEST 2024
spring.application.name=migrate
spring.batch.job.enabled=false

api.base.url=${API_OLD_URL}
api2.base.url=${API_NEW_URL}

api.security.base.url=${API_SECURITY_URL}
api.security.user = ${API_SECURITY_USER}
api.security.password = ${API_SECURITY_PASSWORD}

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
